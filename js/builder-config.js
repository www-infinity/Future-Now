// Builder configuration — generated at deploy time by GitHub Actions.
// The GHP_TOKEN secret is injected here by the workflow so the builder can
// commit pages without requiring the user to enter a token in the UI.
//
// NOTE: This placeholder file (empty token) is committed to git for local
// development. The deployed version — with the real token written by the
// "Inject builder config" workflow step — is NOT committed to git.
window.__BUILDER_CFG = { ghp: '' };
